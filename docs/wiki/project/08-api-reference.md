# 08. API Reference

## CLI Reference

ConfigStream is driven by the `configstream` CLI.

### `merge`
Fetch, test, and merge proxies from sources.
```bash
configstream merge --sources sources/batch_1.txt --output output/ --max-workers 50
```
**Options**
- `--sources`: Path to source list (required).
- `--output`: Output directory (default: `output/`).
- `--max-workers`: Concurrency limit (0 = auto-scale).
- `--timeout`: Test timeout in seconds (defaults to `TEST_TIMEOUT`).
- `--country`: ISO country code filter (e.g., `US`).
- `--max-latency`: Max acceptable latency in ms.
- `--leniency/--strict`: Allow insecure proxies (default: strict).
- `--dry-run`: Skip network tests.
- `--verbose`: Debug logging.

### `retest`
Retest proxies from an existing `proxies.json`.
```bash
configstream retest --input output/proxies.json --output output/ --max-workers 50
```

### `update-databases`
Download GeoIP databases.
```bash
configstream update-databases
```

### `generate-warp`
Generate WARP templates.
```bash
configstream generate-warp --count 1
```

### `bot`
Start the Telegram bot (polling).
```bash
configstream bot --token $TELEGRAM_BOT_TOKEN
```

### `backup`
Backup pipeline databases.
```bash
configstream backup --days 7 --dir data
```

## Data Architecture

ConfigStream is a **static site**. The pipeline generates files into `output/`, the deploy workflow merges frontend assets into the same directory, and GitHub Pages serves everything as flat files. There is no live backend server in the default deployment.

```
Pipeline (Python) → output/ → GitHub Pages Artifact → Static Site
                                    ↓
                        Frontend JS fetches via relative URLs
```

> **Analogy**: Think of it as a newspaper printing press. The pipeline "prints" all the data files every 4 hours. The website is just a newsstand that displays the latest edition. There's no live reporter answering questions — just pre-printed pages.

## REST API

ConfigStream can also run as a live FastAPI server for development or self-hosted deployments.

### Static Site Endpoints (GitHub Pages)

These are the primary endpoints used by the frontend. They are flat files, not dynamic APIs.

#### Frontend Function Mapping

| Frontend Function | Primary URL | Fallback URL | JS Module |
|---|---|---|---|
| `fetchMetadata()` | `metadata.json` | `/api/stats` | `utils/network.js` |
| `fetchProxies()` | `proxies.json` | `/api/proxies` | `utils/network.js` |
| `fetchStatistics()` | `metadata.json` | — | `statistics.js` |
| `fetchProxyHistory()` | `data/active_proxy_trend.json` | — | `statistics.js` |
| `fetchEvasionTrend()` | `data/evasion_trend.json` | — | `statistics.js` |
| `loadCountryData()` | `assets/data/countries.json` | — | `statistics.js` |

#### API Simulation (created by `deploy-pages.yml`)

| URL | Source | Purpose |
|---|---|---|
| `/api/proxies` | Copy of `proxies.json` | Fallback for `fetchProxies()` |
| `/api/stats` | Copy of `metadata.json` | Fallback for `fetchMetadata()` |
| `/health_check` | Does not exist on static | Failure triggers IPFS failover (expected) |
| `/ws/updates` | Disabled on static hosting | WebSocket — only available with live server |
| `/api/diff/proxies` | Only with live server | Differential updates |

#### Download Files Matrix

All download files are generated by `output_logic.generate_categorized_outputs()`:

| Client | Standard | DNS-Safe | DNS-Hardened |
|---|---|---|---|
| **Sing-box** | `singbox.json` | `singbox-dns-safe.json` | `singbox-dns-hardened.json` |
| **Sing-box VPN** | `singbox-vpn.json` | `singbox-vpn-dns-safe.json` | `singbox-vpn-dns-hardened.json` |
| **Clash** | `clash.yaml` | `clash-dns-safe.yaml` | `clash-dns-hardened.yaml` |
| **Base64** | `base64.txt` | `base64-dns-safe.txt` | `base64-dns-hardened.txt` |
| **Plain text** | `proxies.txt` | `proxies-dns-safe.txt` | `proxies-dns-hardened.txt` |
| **Shadowrocket** | `shadowrocket.txt` | `shadowrocket-dns-safe.txt` | `shadowrocket-dns-hardened.txt` |
| **Surge** | `surge.conf` | `surge-dns-safe.conf` | `surge-dns-hardened.conf` |
| **Loon** | `loon.conf` | `loon-dns-safe.conf` | `loon-dns-hardened.conf` |
| **Quantumult X** | `quantumult.conf` | `quantumult-dns-safe.conf` | `quantumult-dns-hardened.conf` |
| **SIP008** | `sip008.json` | `sip008-dns-safe.json` | `sip008-dns-hardened.json` |
| **Chains (Gold)** | `chains.json` | `chains-dns-safe.json` | `chains-dns-hardened.json` |
| **Side Products** | `side_products.zip` | `side_products-dns-safe.zip` | `side_products-dns-hardened.zip` |

**Example** — Fetch metadata:
```bash
curl -s https://your-site.github.io/metadata.json | python -m json.tool
```

**Example** — Download Sing-box config:
```bash
curl -sO https://your-site.github.io/singbox.json
```

**Example** — Get DNS-hardened Clash config:
```bash
curl -sO https://your-site.github.io/clash-dns-hardened.yaml
```

### Live Server Endpoints (FastAPI)

When running as a live server (`uvicorn configstream.api:app`), these dynamic endpoints are available:

#### `GET /api/stats`
Returns the latest pipeline metadata.
```bash
curl -s http://localhost:8000/api/stats | jq '.total_valid_proxies'
```

#### `GET /api/proxies`
Returns the full proxy list. Supports query filters:
```bash
# Filter by country
curl -s "http://localhost:8000/api/proxies?country=US" | jq length

# Filter by protocol
curl -s "http://localhost:8000/api/proxies?protocol=vless" | jq '.[0]'
```

#### `GET /api/diff/proxies?base_version=...`
Returns a delta against a previous snapshot. Used for incremental updates.

#### `GET /subscribe/{format}`
Dynamic subscription endpoint. Supported formats:
```
base64, clash, singbox, singbox-vpn, singbox-chains, chains,
shadowrocket, quantumult, surge, loon, sip008, revived,
proxies, proxies-json, side-products
```
```bash
curl -s http://localhost:8000/subscribe/clash > clash.yaml
curl -s http://localhost:8000/subscribe/base64 > sub.txt
```

#### `GET /chosen/base64.txt`
Top picks per protocol (small curated Base64 subscription).

#### `GET /health`
Health check endpoint. Returns `{"status": "ok"}`.

#### `WS /ws/updates`
WebSocket stream for real-time pipeline update notifications. Disabled on static hosting.

### Admin Endpoint

#### `POST /api/admin/notify-update`
Broadcast update events to connected WebSocket clients. Requires `ADMIN_API_KEY` header.
```bash
curl -X POST http://localhost:8000/api/admin/notify-update \
  -H "X-API-Key: $ADMIN_API_KEY"
```

---

## Data Formats

### `metadata.json`

Summary statistics consumed by the frontend and downstream clients.

```json
{
  "schema_version": "3.0.2",
  "version": "3.0.2",
  "total_proxies": 4495,
  "total_tested": 12000,
  "total_working": 4300,
  "success_rate": 0.358,
  "generated_at": "2026-02-09T12:00:00Z",
  "last_updated_utc": "2026-02-09T12:00:00Z",
  "total_lines_sourced": 85000,
  "total_unique_candidates": 12000,
  "total_valid_proxies": 4300,
  "total_revived": 650,
  "revived_warp": 400,
  "revived_vwarp": 250,
  "total_clean": 3650,
  "total_dirty": 120,
  "total_smart_chains": 195,
  "smart_chain_count": 195,
  "chain_outbounds_count": 580,
  "shielded_count": 85,
  "vwarp_win_rate": 62.5,
  "vwarp_attempts": 400,
  "vwarp_success": 250,
  "washer_success_count": 320,
  "washing_enabled": true,
  "total_configured_sources": 800,
  "fetched_sources": 780,
  "sources_count": 800,
  "protocols": { "vless": 1200, "trojan": 800, "vmess": 600, "shadowsocks": 400 },
  "country_stats": { "US": 1200, "DE": 300, "NL": 250 },
  "latency_distribution": { "fast": 1500, "medium": 2000, "slow": 700, "very_slow": 100 },
  "latency_by_country": { "US": 140, "DE": 210 },
  "latency_by_protocol": { "vless": 180, "trojan": 220 },
  "asns": { "AS13335": 500, "AS16509": 300 },
  "rejection_reasons": { "duplicate": 5000, "TEST_FAILED": 3200 },
  "evasion_utls_enabled": 3800,
  "evasion_alpn_enabled": 3200,
  "evasion_fragmentation_enabled": 3800,
  "evasion_multiplexing_enabled": 3500,
  "evasion_dns_safe_count": 4300,
  "evasion_dns_hardened_count": 4300,
  "duration_seconds": 420.5,
  "time_limited": false,
  "time_limit_seconds": 0,
  "geo_resolved": 4100,
  "cache_misses": 8000,
  "final_count": 4300,
  "chosen_subset_size": 50,
  "update_interval_hours": 6
}
```

### `proxies.json` (Array Items)

Each proxy object in the array contains:

| Field | Type | Description |
|---|---|---|
| `protocol` | string | Protocol name (e.g., `vless`, `trojan`) |
| `address` | string | Server hostname or IP |
| `port` | int | Server port |
| `latency` / `latency_ms` | float | Measured latency in milliseconds |
| `is_working` | bool | Whether the proxy passed testing |
| `country_code` | string | ISO 3166-1 alpha-2 country code |
| `city` | string | GeoIP city name |
| `asn` | string | Autonomous System Number |
| `org` | string | ASN organization name |
| `config` | string | Original proxy URI (for copy/export) |
| `process` | string | Processing type: `native`, `washed`, `revived-warp`, `revived-vwarp`, `chain` |
| `tags` | array | Feature tags (see Tagging System below) |
| `history` | array | Historical latency values (for sparklines) |

---

## Tagging System

Every proxy carries structured tags in its `tags` array and a human-readable name (remark). Tags follow a `CATEGORY:VALUE` format and are used for filtering, display, and analytics.

### Name (Remark) Format

Default template: `{country_flag} | {stack} | {latency_tag} | {status_tag} | {process_tag} | {issue_tag}`

The `stack` field is a composite label built as `PROTO+TRANSPORT+SECURITY`, for example:
- `VLESS+WS+TLS`
- `TROJAN+GRPC+REALITY`
- `SS2022+TCP+AES-256-GCM`
- `REVIVED-VLESS+WS+TLS+VWARP`

### Core Tags

| Tag | Example | Description |
|---|---|---|
| `PROTO:*` | `PROTO:VLESS` | Protocol name (uppercased) |
| `TRANS:*` | `TRANS:WS` | Transport: `WS`, `GRPC`, `H2`, `TCP`, `KCP`, `QUIC`, `UDP` |
| `SEC:*` | `SEC:REALITY` | Security layer: `TLS` or `REALITY` |
| `PROC:*` | `PROC:NATIVE` | Process: `NATIVE`, `REVIVED-WARP`, `REVIVED-VWARP`, `WASHED`, `SHIELDED` |
| `STATUS:*` | `STATUS:UP` | Working status: `UP` or `DOWN` |
| `GEO:*` | `GEO:🇺🇸` | Country flag emoji |
| `LAT:*` | `LAT:120MS` | Latency in milliseconds |

### Evasion & DNS Tags

| Tag | Meaning |
|---|---|
| `EVASION:UTLS` | [uTLS fingerprint rotation](../encyclopedia/glossary/networking_terms.md) applied |
| `EVASION:FRAG` | [TLS fragmentation](../encyclopedia/glossary/networking_terms.md) applied |
| `EVASION:MUX` | Multiplexing with padding applied |
| `EVASION:ALPN` | [ALPN rotation](../encyclopedia/glossary/networking_terms.md) applied |
| `DNS:SAFE` | Included in DNS-safe (IP-only) output |
| `DNS:HARDENED` | Included in DNS-hardened (DoH/DoT/DoQ) output |

### Revival & Shielding Tags

| Tag | Meaning |
|---|---|
| `REVIVED` | Proxy revived via [WARP/Vwarp chain](../encyclopedia/networking/warp.md) |
| `WARP` / `VWARP` | Revival chain type |
| `SHIELDED` / `GOLD` | [Shielded proxy](../../CENSORSHIP_EVASION.md) (Copper → Gold transformation) |

### Outbound Name Patterns (Sing-box configs)

| Pattern | Meaning |
|---|---|
| `GOLD-{id}` | Shielded proxy (user-facing, in selectors) |
| `SHIELD-{id}` | Shield base WireGuard outbound (internal) |
| `WARP-REVIVE-{id}` | WARP-revived chain |
| `VWARP-REVIVE-{id}` | Vwarp-revived chain |
| `🛡️ Secure-{name}` | Standard washed chain |
| `🛡️⚡ Optimal-{name}` | Optimized washed chain |

### Security Issue Tags

Format: `ISSUE:<CATEGORY>:<REASON>` (e.g., `ISSUE:MALWARE:DNS_LEAK`). Derived from `proxy.security_issues` after normalization.

### Tag Ordering

Tags are appended in this order: `PROTO` → `TRANS` → `SEC` → `PROC` → `REVIVED`/`WARP` → `STATUS` → `GEO` → `LAT` → `ISSUE` → existing tags (deduplicated).

### Example

**Name**: `🇺🇸 | VLESS+WS+TLS | 120ms | UP | NATIVE`

**Tags**: `["PROTO:VLESS", "TRANS:WS", "SEC:TLS", "PROC:NATIVE", "STATUS:UP", "GEO:🇺🇸", "LAT:120MS"]`

---

## Related Documentation

*   **[Protocols & Parsing](03-protocols.md)** — How proxy URIs are parsed and validated.
*   **[Configuration Reference](Configuration.md)** — All environment variables.
*   **[Output Variations](../../OUTPUT_VARIATIONS.md)** — All 60+ output file variants.
*   **[Censorship Evasion](../../CENSORSHIP_EVASION.md)** — Evasion modes, techniques, tagging, metrics, troubleshooting.
*   **[Sing-box Configuration Guide](../encyclopedia/tools/singbox_configuration_guide.md)** — How outbound configs are structured.
