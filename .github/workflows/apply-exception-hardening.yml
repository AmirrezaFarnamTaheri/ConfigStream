name: Exception Hardening Maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired one-time workflow
        run: echo "The exception-hardening transformation has already been applied and validated."
