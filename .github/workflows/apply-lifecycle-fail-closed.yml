name: Lifecycle Remediation Placeholder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lifecycle remediation is applied through reviewed source commits; this placeholder performs no writes."
