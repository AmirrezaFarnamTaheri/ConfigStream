name: CI

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'sources/batch_*.txt'
      - 'sources/backup_dynamic/**'
  pull_request:

# Least-privilege: CI only reads the repository. No job needs write access.
permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
