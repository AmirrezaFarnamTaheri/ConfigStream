// Runtime config generated during Pages deploy.
// Local/offline source defaults intentionally contain no production keys.
(function(global) {
    global.CS_RUNTIME_CONFIG = Object.assign(
        {
            PUBLIC_KEY: "",
            IPNS_KEY: ""
        },
        global.CS_RUNTIME_CONFIG || {}
    );
})(typeof window !== 'undefined' ? window : self);
